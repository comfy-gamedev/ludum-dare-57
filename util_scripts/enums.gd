class_name Enums
extends RefCounted

enum PIP_TYPE {
	ATTACK,
	REND,
	POISON,
	DEFEND,
	ARMOR,
	HEAL,
	REROLL,
	NULLIFY,
}

enum LOCATION_TYPES {
	ENEMY,
	ELITE,
	TREASURE,
	EVENT,
	EMPTY,
	BOSS,
	MUTATION,
	SHOP,
	UPGRADE
}

enum ENEMY_ACTION {
	PASS,
	ATTACK,
}

enum ENEMY_ACTION_MODE {
	LOOP,
	RANDOM,
}

enum EFFECTS {
	ADD_PIP,
	REMOVE_PIP,
	DOUBLE_FACE,
	TAKE_DAMAGE, # ⌄ Only when cannot be done with pips
	DEAL_DAMAGE,
	HEAL,
}

enum TRIGGERS {
	TAKE_DAMAGE,
	DEAL_DAMAGE,
	ROLL_SIDE,
	ROLL_DIE,
	ROLL_BLANK,
	ROLL_UNDER,
	ROLL_OVER
}
